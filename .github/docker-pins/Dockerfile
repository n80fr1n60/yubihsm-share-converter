# H7-M2 + H7-M4: Dockerfile shim — REFERENCE-ONLY, NEVER BUILT.
#
# Dependabot's `docker` ecosystem scans `FROM` directives in this file and
# opens PRs when upstream digests rotate (e.g. base-image CVE rebuilds).
# The release workflow's `container:` keys in .github/workflows/release.yml
# must be kept in sync with the digests below — manual sync on every
# Dependabot PR.

FROM ubuntu@sha256:962f6cadeae0ea6284001009daa4cc9a8c37e75d1f5191cf0eb83fe565b63dd7
FROM ubuntu@sha256:c4a8d5503dfb2a3eb8ab5f807da5bc69a85730fb49b5cfca2330194ebcc41c7b
FROM debian@sha256:e2d08da6f42ef4b09b165d55528a12727aeed8240dc9edf888e3ec07e10ef9da
